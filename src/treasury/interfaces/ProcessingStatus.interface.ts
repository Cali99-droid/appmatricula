/**pending/processing/success/error */
export enum ProcessingStatusInterface {
  default = 'default',
  pending = 'pending',
  processing = 'processing',
  success = 'success',
  error = 'error',
}
